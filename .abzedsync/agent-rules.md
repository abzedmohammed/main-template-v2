# AbzedSync Agent Rules

Before making any implementation changes in this repository, always read:

- `docs/project-apis.md`

Rules:

1. `docs/project-apis.md` is the shared synced source of truth for cross-project API and logic updates.
2. Always check it before implementing any feature, bug fix, refactor, enhancement, or integration change.
3. If `docs/project-apis.md` changed recently, use its latest definitions and notes before writing code.
4. Do not assume API contracts without checking this file first.
5. If your implementation touches an area documented in `docs/project-apis.md`, align your changes with it.

AbzedSync generated file.
Do not delete manually unless AbzedSync configuration changes.
